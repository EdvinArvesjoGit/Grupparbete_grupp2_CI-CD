# Copy to .env and adjust. NEVER commit .env.
PGHOST=localhost
PGPORT=5432
PGDATABASE=riksdag
PGUSER=postgres
PGPASSWORD=changeme

# Scope for extraction — comma separated riksmoten
RIKSMOTEN=2022/23,2023/24,2024/25,2025/26

# Where downloaded source files are cached
DATA_DIR=data/raw
